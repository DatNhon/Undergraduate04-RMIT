"""Launcher for the Smart Path Finder CLI.

This module keeps a minimal wrapper so the program can be executed as
`python main.py` from the project root.
"""

from app import main


if __name__ == "__main__":
    main()
